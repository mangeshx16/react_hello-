runtime: node.js16.14.0
handlers:
 # serve all static files with urls ending with a file extension
 url: /(.*\..+)$
 static_files: build/\1
 upload: buid/(.*\..+)$
 #catch al thge handler to index.html
 url: /.*
 static_files: build/index.html
 upload: build/index.html 

